#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PLUGIN_DIR=$(CDPATH= cd -- "$SCRIPT_DIR/../plugins/claude-fusion-drive" && pwd)
cd "$PLUGIN_DIR"
exec python3 -m relentless_inception "$@"
